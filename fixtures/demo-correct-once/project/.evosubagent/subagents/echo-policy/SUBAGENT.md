---
name: echo-policy
description: Use when a demo needs a subagent that answers with a fixed line prefix so evolution can be observed.
---

# Echo Policy

When answering any task, start the first line with exactly `OLD:`.
Then restate the task briefly.
