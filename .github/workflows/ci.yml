name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Unit tests
        run: npm test
      - name: Demo correct-once
        run: npm run demo:correct-once
      - name: Mini hermetic eval
        run: npm run eval:mini
